export function Colors({ type, data }) {
  if (type == "fire") {
    const pushElement = [
      {
        id: uuid(),
        type: a.type,
        color: color,
        text: "text-white",
      },
    ];
  }
}
